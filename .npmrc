# Puppeteer is only used by the local screenshot script (scripts/screenshot-sites.mjs),
# which drives your already-installed system Chrome. Skip the bundled Chromium download
# so `npm install` / `npm ci` stays fast and never fails in CI.
puppeteer_skip_download=true
